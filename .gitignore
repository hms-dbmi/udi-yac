node_modules/
dist/
dist-ssr/
.quasar/
*.log
logs/
.env
.env.local
.env.local.*
*.local
__pycache__/
*.py[cod]
.venv/
.DS_Store
.idea/
*storybook.log
storybook-static/

sample-data/pcx
# Derived from sample-data/pcx by scripts/normalize_cube_time.py — same private
# clinical data, so it stays out of this public repo just like its source.
sample-data/pcx_efs_cube
# Written by packages/agent/scripts/seed_starrocks.py (contains local connection details)
packages/agent/starrocks-backends.json
# Written by packages/agent/scripts/seed_duckdb.py (local backend config + seeded DB file)
packages/agent/duckdb-backends.json
packages/agent/*.duckdb

# Claude Code: repo-local skills under .claude/skills/ are tracked; local
# scratch state is not.
.claude/worktrees/
.claude/settings.local.json
