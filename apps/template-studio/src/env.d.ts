/// <reference types="vite/client" />

// The `udi-vis` custom element is created by `UDIVis` from `udi-toolkit/react`
// via createElement, never written as JSX here, so it needs no IntrinsicElements
// augmentation. It is styled by element name in index.css.
