{
  "compilerOptions": {
    // `baseUrl` was deprecated in TS 6 — `paths` resolves relative to the
    // tsconfig file's directory on its own now.
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}
