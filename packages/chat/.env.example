# Standalone-app config (read in src/App.tsx). Copy to .env.local to override.
# All variables are optional — App.tsx ships sensible defaults for local dev.

# UDIAgent FastAPI server URL.
VITE_UDI_API_BASE_URL=http://localhost:8007

# Optional override for the data source. When unset, the standalone app
# defaults to the inline HuBMAP DataPackage in src/data/hubmapRemote.ts which
# fetches data from the live HuBMAP Portal API. Set this to point at a local
# bundled file (e.g. /data/hubmap_2025-05-05/datapackage_udi.json, served by
# Vite from public/data/) or any other URL the backend can reach.
# VITE_UDI_DATA_PACKAGE=/data/hubmap_2025-05-05/datapackage_udi.json

# Set to "false" to skip the in-app OpenAI key prompt (e.g. when the backend
# supplies its own credentials). Default: prompt enabled.
VITE_UDI_REQUIRE_API_KEY=true

# Optional LLM model name override forwarded to the backend.
# VITE_UDI_MODEL=agenticx/UDI-VIS-Beta-v2
